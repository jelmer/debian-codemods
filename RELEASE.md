# Releasing

Before tagging a release, make sure that:

* `debian/changelog` reflects the new version number
* `Cargo.toml` reflects the new version number
* the `master` pipeline is green for the commit being released
